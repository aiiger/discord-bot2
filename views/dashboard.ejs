<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FACEIT Bot Dashboard</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1a1a1a;
            color: #ffffff;
            margin: 0;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        .container {
            background-color: #2d2d2d;
            padding: 30px;
            border-radius: 8px;
            max-width: 600px;
            width: 100%;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        h1 {
            color: #ff5500;
            text-align: center;
            margin-bottom: 30px;
        }

        .status-box {
            background-color: #363636;
            padding: 20px;
            border-radius: 4px;
            margin-bottom: 20px;
        }

        .status-box h2 {
            color: #ff5500;
            margin-top: 0;
        }

        .user-info {
            margin-top: 20px;
            padding: 15px;
            background-color: #404040;
            border-radius: 4px;
        }

        .user-info p {
            margin: 5px 0;
        }

        .success-message {
            background-color: #4CAF50;
            color: white;
            padding: 15px;
            border-radius: 4px;
            margin-bottom: 20px;
            text-align: center;
        }

        .button {
            background-color: #ff5500;
            color: white;
            border: none;
            padding: 12px 24px;
            border-radius: 4px;
            cursor: pointer;
            font-size: 16px;
            display: block;
            margin: 20px auto;
            text-decoration: none;
            text-align: center;
            transition: background-color 0.3s;
        }

        .button:hover {
            background-color: #ff6a1f;
        }

        .button.logout {
            background-color: #f44336;
        }

        .button.logout:hover {
            background-color: #ff5c4e;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>FACEIT Bot Dashboard</h1>

        <div class="success-message">
            Authentication Successful!
        </div>

        <div class="status-box">
            <h2>Status</h2>
            <p>Bot is now authenticated and ready to send messages in match rooms.</p>
        </div>

        <div class="user-info">
            <h2>Account Information</h2>
            <p><strong>Username:</strong>
                <%= username %>
            </p>
            <% if (userInfo) { %>
                <p><strong>Email:</strong>
                    <%= userInfo.email %>
                </p>
                <p><strong>FACEIT ID:</strong>
                    <%= userInfo.guid %>
                </p>
                <% } %>
        </div>

        <a href="/logout" class="button logout">Logout</a>
    </div>

    <script>
        // Send a message to the parent window if this was opened in a popup
        if (window.opener) {
            window.opener.postMessage('authentication-success', '*');
        }
    </script>
</body>

</html>