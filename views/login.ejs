<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Link FACEIT Account</title>
	<style>
		body {
			font-family: Arial, sans-serif;
			background-color: #1a1a1a;
			color: #ffffff;
			margin: 0;
			padding: 20px;
			display: flex;
			justify-content: center;
			align-items: center;
			min-height: 100vh;
		}

		.container {
			background-color: #2d2d2d;
			padding: 30px;
			border-radius: 8px;
			max-width: 600px;
			width: 100%;
			box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
		}

		h1 {
			color: #ff5500;
			text-align: center;
			margin-bottom: 30px;
		}

		.steps {
			margin-bottom: 30px;
		}

		.steps ol {
			padding-left: 20px;
		}

		.steps li {
			margin-bottom: 10px;
			color: #cccccc;
		}

		.auth-button {
			background-color: #ff5500;
			color: white;
			border: none;
			padding: 12px 24px;
			border-radius: 4px;
			cursor: pointer;
			font-size: 16px;
			display: block;
			margin: 0 auto;
			text-decoration: none;
			text-align: center;
			transition: background-color 0.3s;
		}

		.auth-button:hover {
			background-color: #ff6a1f;
		}

		.instructions {
			margin-top: 30px;
			padding: 20px;
			background-color: #363636;
			border-radius: 4px;
		}

		.instructions h2 {
			color: #ff5500;
			margin-top: 0;
		}

		.note {
			margin-top: 20px;
			color: #ff5500;
		}

		.status {
			text-align: center;
			margin-top: 20px;
			padding: 10px;
			border-radius: 4px;
		}

		.status-authenticated {
			background-color: #4CAF50;
			color: white;
		}

		.status-not-authenticated {
			background-color: #f44336;
			color: white;
		}
	</style>
</head>

<body>
	<div class="container">
		<h1>Link Your FACEIT Account</h1>

		<div class="steps">
			<ol>
				<li>Click the button below to start account linking</li>
				<li>Authorize access to your FACEIT account</li>
				<li>Return to complete the linking process</li>
			</ol>
		</div>

		<a href="/auth/faceit" class="auth-button">Link FACEIT Account</a>

		<div class="instructions">
			<h2>How to Link Your Account:</h2>
			<ol>
				<li>Click the "Link FACEIT Account" button above</li>
				<li>Log in to your FACEIT account if not already logged in</li>
				<li>On the FACEIT authorization page, click "Allow" or "Authorize"</li>
				<li>Wait to be redirected back to this application</li>
			</ol>

			<div class="note">
				Note: Make sure to complete the authorization on FACEIT's website when redirected.
			</div>
		</div>

		<div class="status <%= authenticated ? 'status-authenticated' : 'status-not-authenticated' %>">
			Status: <%= authenticated ? 'Authenticated' : 'Not Authenticated' %>
		</div>
	</div>
</body>

</html>